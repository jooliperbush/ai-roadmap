# Production deployment — 5 September 2026

- Deployed application commit: `cdecd8e` via Railway CLI upload of `answerops/` as the archive root.
- Railway project: `answerops` (`f340254b-73ad-4ff4-93f1-ac7e3c991724`).
- Service: `miscited` (`52bb09c4-9365-440f-a5d1-6d53a37167d7`), production.
- Deployment: `2fc12c7e-fdc9-4cf2-9d37-aa83f76fc62d`, status SUCCESS.
- Public domains: https://miscited.com and https://www.miscited.com.
- Existing persistent volume remains mounted at `/data`; application startup confirmed port 8080 and `/data/miscited.sqlite`.
- Previous deployment: `1f4d341f-b0c7-4d47-ac8a-e3cbe6cfa0dc`.

## Verification

Both public hosts returned HTTP 200 with the new homepage copy. `/healthz`, `/login`, `/blog` and the marketing assets returned HTTP 200. Live CSS, JavaScript and launch-card SHA-256 hashes matched local committed files. A real Chromium session loaded the public site with no JavaScript errors and followed the primary CTA to the audit form. No audit submission or live-provider spend was generated by this verification.

Railway built with `npm ci` successfully. Existing local validation for the deployed commit: 1,034 unit/integration tests, 29 browser journeys and TypeScript checks passed.

## Operational notes

No provider API keys appeared in the service variable configuration at deployment time. The website is public, but unconfigured audits remain explicitly labelled simulations. Real provider results require operator configuration.

The production dependency audit reports two high-severity affected packages: `@fastify/static` and `fast-uri`, encompassing multiple advisories. These pre-existing dependencies were not changed during this deployment; they need a separately tested dependency update. The deployment is healthy, but this is not a security-clearance claim. See `npm audit --omit=dev` for current details.

The existing authenticated Railway CLI was used; the separately connected Railway app lacked access to this project. No repository push, rename or external announcement occurred. SSH backup was unavailable because no SSH identity was configured; no new backup is claimed.

## Traffic-first update

Application commit `2735aba` was subsequently deployed as `f6dfadf9-5aee-4d47-8c87-f8a73880ec38` (SUCCESS). Includes additive migration 010 for aggregate launch counts, referral attribution and demo-first activation. TypeScript checks, 1,038 application tests and 31 browser journeys passed before deployment. Live Chromium verification confirmed the new CTA navigates to the worked example, no JavaScript errors, and HTTP 200 health. Do Not Track was enabled during verification to avoid adding optional analytics events. No live audit, paid traffic or external announcement was initiated.

## OpenAI configuration check

User added `OPENAI_API_KEY` in Railway. Deployment `dda25c06-f6c3-49a8-bc00-d8b3410b83a1` reached SUCCESS. Homepage and health returned HTTP 200. The existing adapter was tested with one small grounded request; OpenAI returned HTTP 429. A minimal diagnostic request identified `insufficient_quota` / `credit_balance_exhausted`: no API credits remaining. No successful live answer or end-to-end live audit is claimed. No credentials were printed or saved in the repository. The key remains configured for use after the user funds the provider account. Homepage CTA currently reflects key presence, not verified provider quota.
